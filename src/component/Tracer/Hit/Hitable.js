export default class Hitable {
  constructor() {}

  didHit(ray, tMin, tMax, hitRecord) {
    ray;
    tMin;
    tMax;
    hitRecord;
  }

  getBoundingBox(t0, t1, aabb) {
    t0;
    t1;
    aabb;
  }
}
